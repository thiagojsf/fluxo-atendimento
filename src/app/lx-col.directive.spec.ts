import { LxColDirective } from './lx-col.directive';

describe('LxColDirective', () => {
  it('should create an instance', () => {
    const directive = new LxColDirective(null);
    expect(directive).toBeTruthy();
  });
});
